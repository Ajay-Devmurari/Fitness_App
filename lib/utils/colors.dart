import 'dart:ui';

import 'package:flutter/material.dart';

class CustomColors {
  // use for custom button
  static const Color customButtonBgColor = Color.fromARGB(
    255,
    0,
    0,
    0,
  );

  static const Color customButtonFontColor = Color.fromARGB(
    255,
    255,
    255,
    255,
  );

  // use for appbar color
  static const Color appbarColor = Colors.transparent;

  //use for iconColo(ActivityScreen)
  static const Color iconColor = Color.fromARGB(
    255,
    255,
    255,
    255,
  );

  // use for UserScreen
  static Color? textColor = Colors.grey[700];

  // use for CustomSlider
  static const Color bgColor = Color.fromARGB(
    255,
    158,
    158,
    158,
  );
  static const activeColor = Color.fromARGB(
    255,
    33,
    150,
    243,
  );
  static const sliderTextColor = Color.fromARGB(
    255,
    255,
    255,
    255,
  );
  static const hwTextColor = Color.fromARGB(
    255,
    255,
    255,
    255,
  );
}
